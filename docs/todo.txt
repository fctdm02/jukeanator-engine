TASK NAME
=========
Location/SongLibrary object model and JPA mapping refactor


OVERVIEW
========
1: The key conceptual change here is that when it comes to standalone/slave mode, a song library will always
   have a "parent" location and a location will always have a "child" song library.

2: For standalone/slave mode, RootFolderEntity and LocationEntity will have a 1-to-1 association via transient attribute linkage:
   a. LocationService to be injected into SongLibraryService
   b. SongLibraryService gets default LocationEntity
   c. SongLibraryService sets location on song library root
   d. SongLibraryService sets song library on location

2: As is currently implemented, SongLibraryServiceImpl has these two attributes:
  private RootFolderEntity ownRoot;
  private Integer ownLocationId;

  that will be changed to:
  private LocationEntity ownLocation;
  private RootFolderEntity ownRoot;

  When the application is started for the first time in standalone/slave mode, default LocationEntity
  and SongLibraryRootEntity instances should be created and associated to one another as parent/child.

3: LocationMetadataFileEntity will go away, as this information will be stored with LocationEntity.

4: A location's information can be edited by a new "Edit Location Info" button on the UI Admin Panel.

5. The assumption is that for a new standalone/slave system, the LocationEntity is instantiated first (as well as the LocationService)

6: SongLibraryService would change in that the LocationService would be injected, and the "ownLocation" LocationEntity instance would be 
   retrieved from it and associated with the RootFolderEntity instance (and vice-versa)  For "ownership" of the relationship from a 
   JPA perspective, it would have to be the "parent" LocationEntity
   
7: In Domain Driven Design parlance, the "aggregate root" would therefore be the LocationEntity, but realistically, the transactional boundary is
just to the RootFolderEntity, as the information about a location is independent of the song library.  For JPA impl, as well as for thje web app, 
the locationId is needed, however.   
   
QUESTIONS: 

1: For the current implementation, why does LocationService need a dependency on the SongLibraryService?  If it is because of the need to
get the RootFolderEntity?  Because of the workflow above, the RootFolderEntity is associated with the LocationEntity at the time of loading/creating 
the RootFolderEntity.


FUTURE STATE OBJECT INHERITANCE HIERARCHY
=========================================
Object Classes

1:AbstractEntity extends Object

2:AbstractPersistentEntity extends AbstractEntity
  Integer persistentIdentity
  Integer version = 1;

3:LocationEntity extends AbstractPersistentEntity
  private String name = "Rock On Third";
  private String logoName = "RockOnThirdLogo.jpg";
  private Double latitude = 42.4883;
  private Double longitude = -83.143;
  private boolean isGeoFenced = true;
  private String apiKeyHash;
  private LocationStatus status = LocationStatus.PENDING;
  private Instant lastSeenAt;
  private Instant libraryLastSyncedAt;
  private transient RootFolderEntity locationSongLibraryRoot;

4:AbstractLibraryEntity extends AbstractPersistentEntity
  private FolderEntity parentFolder;
  private String name;
  private Integer id;  
 
5:FolderEntity extends AbstractLibraryEntity
  private Set<FolderEntity> childFolders = new TreeSet<FolderEntity>();

6:GenreFolderEntity extends FolderEntity

7:ArtistFolderEntity extends FolderEntity

8:ArtistFromSongEntity extends ArtistFolderEntity
  private final Set<AlbumFolderEntity> childAlbums = new TreeSet<AlbumFolderEntity>();

9:AlbumFolderEntity extends FolderEntity
  private AlbumCoverArtFileEntity coverArt;
  private AlbumMetaDataFileEntity metaData;
  private Set<SongFileEntity> childSongs = new TreeSet<SongFileEntity>();
 
10:RootFolderEntity extends FolderEntity
  private transient LocationEntity parentLocation;
  private Set<ArtistFromSongEntity> artistsFromSongs = new TreeSet<ArtistFromSongEntity>();

11:AbstractFileEntity extends AbstractLibraryEntity

12:AlbumCoverArtFileEntity extends AbstractFileEntity

13:AlbumMetaDataFileEntity extends AbstractFileEntity
  private String genre = "";
  private String coverArtUrl = "";
  private String recordLabel = "";
  private String releaseDate = "";
  private boolean hasExplicit = false;

14:SongFileEntity extends AbstractFileEntity
  private Integer numPlays = Integer.valueOf(0);
  private String artistName;
  private String songName;
  private Integer trackNumber;




FUTURE STATE OBJECT TO RELATIONAL MAPPING STRATEGY
==================================================
NOTES: 
a. The "persistentIdentity" class attribute should always map to an "id" JPA/table column name
b. For the Song Library inheritance hierarchy, the persistent identity set during the song scan
   will be unique and should be saved as-is to the song_library table.  That is, there will be no
   need for this "sourceId" property in SongLibraryFolderJpaEntity and SongLibraryFileJpaEntity
   (In addition location metadata will no longer exist and cover art/album metadata will not be mapped).
c. The unique constraint for a table should always match getNaturalIdentity() for class
d. AlbumMetaDataFileEntity does not need to be mapped to the database, as all the attributes of this
   class are contained in the ALBUM_METADATA COLUMNS that would be stored directly with the album 
   row. This class can be re-hyrdated by instantiating the class and setting these properties.
e. AlbumCoverArtFileEntity does not need to be mapped to the database, as the only meaningful attribute
   of this class is a hardcoded value for "name", which is always going to be "cover.jpg".  The full path
   of this cover art image file can be derived by using the full path of the associated album. This
   class can be re-hydrated by instanting the class and passing in the album as a parent and the constant
   that is used for "cover.jpg".


Table Definitions:
==================
1: 
table name:
    location
maps classes: 
    AbstractEntity, 
    AbstractPersistentEntity, 
    LocationEntity
columms:
    id                      INT PRIMARY KEY,
    version                 INT,
    name                    VARCHAR(255) NOT NULL,
    latitude                DOUBLE,
    longitude               DOUBLE,
    api_key_hash            VARCHAR(255) NOT NULL,
    status                  VARCHAR(255) NOT NULL,
    last_seen_at            TIMESTAMP NULL,
    library_last_synced_at  TIMESTAMP NULL,
    logo_name               VARCHAR(255),
    is_geo_fenced           BOOLEAN,
primary key constraint:
    id
foreign key constraints:
    NONE DEFINED    
unique constraint:
    name


2: 
table name:
    song_library
maps classes: 
    AbstractEntity, 
    AbstractPersistentEntity, 
    AbstractLibraryEntity, 
    FolderEntity, 
    GenreFolderEntity, 
    ArtistFolderEntity, 
    ArtistFromSongEntity, 
    AlbumFolderEntity, 
    RootFolderEntity,
    AbstractFileEntity, 
    SongFileEntity
columms:
    -- COMMON COLUMNS
    id                      INT PRIMARY KEY,
    version                 INT,
    name                    VARCHAR(255) NOT NULL,
    parent_location_id      INT NOT NULL,
    parent_folder_id        INT NOT NULL,

    -- CLASS DISCRIMINATOR COLUMN 
    -- COLUMN VALUE -> CONCRETE CLASS
    -- ===========================================
    -- ROOT                -> RootFolderEntity
    -- FOLDER              -> FolderEntity
    -- GENRE               -> GenreFolderEntity
    -- ARTIST              -> ArtistFolderEntity
    -- SONG_ARTIST         -> ArtistFromSongEntity
    -- ALBUM               -> AlbumFolderEntity
    -- SONG                -> SongFileEntity
    class_discriminator    VARCHAR(20) NOT NULL,   

    -- SONG COLUMNS
    song_artist_name        VARCHAR(500),
    song_name               VARCHAR(500),
    song_track_number       INT,
    song_num_plays          INT,

    -- ALBUM_METADATA COLUMNS
    album_genre             VARCHAR(255),
    album_cover_art_url     VARCHAR(1000),
    album_record_label      VARCHAR(255),
    album_release_date      VARCHAR(20),
    album_has_explicit      BOOLEAN
primary key constraint:
    parent_location_id,id
foreign key constraints:
    parent_location_id references location(id)
    parent_folder_id references song_library_folder(id)
unique constraint:
    parent_location_id,,name




Formulate a plan and ask any questions first.  I expect this to be back and forth, given the major changes that will be needed here.


Gaps and things I need decisions on

1. The song_library.id uniqueness problem (blocking). Your table design makes id a plain single-column PRIMARY KEY, and note (b) says the scan-assigned persistentIdentity 
should be saved as-is, with no separate sourceId. But SongScanner.java assigns persistentIdentity from five separate counters (folderIndex, genreIndex, artistIndex, 
albumIndex, songIndex), each reset to 0 per scan. So today, within one location, a genre, an artist, an album, and a song can all legitimately have persistentIdentity = 0 
simultaneously — and every location's scan restarts at 0 too. That can't be a single-column global PK as-is.To make note (b) work, I see two options:

(a) Give SongScanner one shared counter across all folder/file types per scan, and make id effectively (location_id, id)-unique in practice even though the column is a bare PK
 — meaning each location's tree still needs a way to avoid colliding with other locations' rows in the same physical column. That still doesn't give a true single-column global PK across locations.
 
(b) Keep a real surrogate PK (GenerationType.SEQUENCE off the shared persistent_identity_seq, same mechanism used everywhere else in this codebase) as id, and accept that 
this is the "sourceId" role folded into one column instead of two — i.e., we drop the redundant table-row surrogate id we have today (the current schema's persistent_identity PK is a row-insertion-order id, separate from source_id), but the domain object's own persistentIdentity field would need to actually become that sequence-generated value (assigned at persist time, not scan time) rather than the scan-local index it is today.

Option (b) is a bigger behavioral change: it means a freshly-scanned RootFolderEntity held in memory (before it's ever persisted) has persistentIdentity == null for every folder/song 
until storeAggregateRoot runs and Hibernate assigns real ids — which the filesystem-repository code path and a lot of existing scan logic assumes is populated immediately after scanFileSystemForSongs() returns. Which of these did you intend, or is there a third approach I'm missing?

DECISION 1:
<location_id,id,class_discriminator> will give uniqueness.  Also, start each index at 1.  Please verify that you understand that <location_id,id,class_discriminator> will give uniqueness.


2. LocationEntity.locationName isn't in your own table's column list. Your class lists both name and locationName = "Rock On Third", but the location table's column list 
has only name (no location_name column). Is locationName meant to be dropped (with name taking over as the display name, defaulting to "Rock On Third"), or did the 
table definition just miss a column? Same question doesn't apply to logoName/latitude/longitude/isGeoFenced — those all appear in both the class and the table.
DECISION 2:
LocationEntity.locationName was a typo, and it should be removed/ignored, as a location's "name" does the same thing (and it is an inherited property) 


3. New UNIQUE(name) constraint on location. Today's locations table (V2 migration) has no unique constraint on name, and registerLocation() doesn't check for a duplicate 
name before inserting. Adding the constraint is easy; do you want registerLocation() (and the new default-location bootstrap for standalone/slave) to pre-check and return 
a clean validation error, or is relying on the DB constraint throwing (and turning that into an error path) good enough for now?
DECISION 3: 
There is no need for a pre-validation check, as the number of locations will be very small and location names will be easy to manage at each individual location, so relying
upon the DB constraint throwing an error is good enough for now. 
 





 

With respect to the primary key constraint and unique constraint for the song_library table:

1: Currently, every entity in the RootFolderEntity inheritance hierarchy, with AbstractLibraryEntity being the base class, extends from AbstractPersistentEntity.
AbstractPersistentEntity has the following attribute defined:
private Integer persistentIdentity;

this maps to the database using persistent_identity_seq.  So, the issue here with song_library means that AbstractPersistentEntity is not a good choice for base class.


There is another class, called AbstractAssociativeEntity, that was meant for dealing with what I am trying to accomplish, that is, having a song library item having a
composite primary key.  AbstractAssociativeEntity has the following defined:  
public abstract Map<String, Integer> persistentIdentity();

The design of this class is to allow for entities that will have a composite primary key when mapped to the database.  So, I propose the following change for AbstractLibraryItemEntity: 

public abstract class AbstractLibraryEntity extends AbstractAssociativeEntity {
  private FolderEntity parentFolder;
  private String name;
  private Integer id;

  @Override
  public Map<String, Integer> getPersistentIdentity() {
    
    Map<String, Integer> persistentIdentity = new LinkedHashMap<>();
    persistentIdentity.addAll(parentFolder.getPersistentIdentity());
    persistentIdentity.put("id", id);
    return persistentIdentity;
  }
  
  @Override
  public String getNaturalIdentity() {
    
    return new StringBuilder()
        .append(parentFolder.getNaturalIdentity())
        .append(AbstractEntity.NATURAL_IDENTITY_DELIMITER)
        .append(name)
        .toString();
  }  

RootFolderEntity would then have these methods defined:

  @Override
  public Map<String, Integer> getPersistentIdentity() {
    
    Map<String, Integer> persistentIdentity = new LinkedHashMap<>();
    persistentIdentity.addAll(parentLocation.getPersistentIdentity());
    persistentIdentity.put("id", id);
    return persistentIdentity;
  }
  
  @Override
  public String getNaturalIdentity() {
    
    return new StringBuilder()
        .append(parentLocation.getNaturalIdentity())
        .append(AbstractEntity.NATURAL_IDENTITY_DELIMITER)
        .append(name)
        .toString();
  }  

NOTE: getPersistentIdentity() deals with the repository/database primary key constraint and getNaturalIdentity() deals with offering a "business key" or surrogate key,
and is usually used to have a user-interpretable way of identifying an identity.  This natural identity then maps to the database as a UNIQUE constraint. 

So, getting back to decision #1, I want to use a shared counter across all folder/file types per scan for SongScanner.  This would be the "id" field defined in AbstractLibraryEntity.

The primary key and unique constraints for song_library table would then be:
primary key constraint:
    parent_location_id,parent_folder_id,id
    
unique constraint:
    parent_location_id,parent_folder_id,name

NOTE: Again, "id" would be a unique integer across an entire song scan, using a shared counter, negating the need for using class_discriminator, as well as addressing the issue
with song unqiueness. 

Let me know if this all makes sense and ask any questions before formulating a plan.










My answers are inline with your questions:
I read AbstractAssociativeEntity.java — it's already in the codebase exactly as you described (abstract Map<String,Integer> getPersistentIdentity(), equals/hashCode built on that map, 
a needsPersisting flag, its own addChild variant) and currently has zero concrete subclasses, so wiring AbstractLibraryEntity into it doesn't collide with anything else. That 
confirms your design is sound at the base-class level. A few things I need pinned down before this becomes a plan:

1. This changes the method signature on getPersistentIdentity(), and that ripples wide. Today, AbstractLibraryEntity's whole subtree (FolderEntity, GenreFolderEntity, ArtistFolderEntity, 
ArtistFromSongEntity, AlbumFolderEntity, SongFileEntity, AbstractFileEntity) inherits Integer getPersistentIdentity()/void setPersistentIdentity(Integer) from AbstractPersistentEntity, 
and it's called as a plain Integer in ~40 places, not just inside songlibrary — SongLibraryMapper, SongQueueMapper, SongQueueServiceImpl, SongQueueRepositoryJpaImpl (which does 
song.getAlbum().getPersistentIdentity(), song.getPersistentIdentity() on library entities reached through the queue), RootFolderEntity, AlbumFolderEntity, FolderEntity, SongScanner, 
SongLibraryServiceImpl, plus their tests. Once getPersistentIdentity() returns Map<String,Integer> for this hierarchy, every one of those breaks and needs to move to a new scalar 
accessor — presumably getId()/setId(Integer), matching the private Integer id; field you sketched. Confirm getId()/setId(Integer) as the names? (Location/User/etc. stay on 
AbstractPersistentEntity and keep getPersistentIdentity()/setPersistentIdentity() as-is — only the song-library hierarchy gets the rename.)
ANSWER 1:
Yes, I am confirming that I want to use getId()/setId(Integer) as the method names.


2. The RootFolderEntity → LocationEntity map seam has a type mismatch. Your sketch does persistentIdentity.addAll(parentLocation.getPersistentIdentity()), but LocationEntity 
stays on AbstractPersistentEntity (per decisions 2/3) — its getPersistentIdentity() returns a plain Integer, not a Map, so there's nothing to merge in that shape. I'll instead 
do persistentIdentity.put("location_id", parentLocation.getPersistentIdentity()) — one entry seeded from Location's scalar id. Confirm "location_id" as the key name (matches the 
parent_location_id column)? And for AbstractLibraryEntity's own version, the merge call should be putAll (Map doesn't have addAll — that's a Collection method) — I'll just use the 
right one when I write it, flagging only in case the method name mattered to you for some other reason.
ANSWER 2:
Yes, I am confirming that "location_id" be used as the key name (matches the parent_location_id column)


3. The composite PK as (parent_location_id, parent_folder_id, id) is one column more than it needs to be, and it breaks for ROOT. Since id is now unique across the entire scan for a
location (shared counter, your decision), (parent_location_id, id) is already sufficient for uniqueness on its own — parent_folder_id doesn't add anything to the PK, it's just an 
FK column. And the original DDL declared parent_folder_id INT NOT NULL, but the ROOT row has no parent folder (RootFolderEntity.getParentFolder() throws today) — so if 
parent_folder_id is genuinely part of the primary key, ROOT can't have a row at all under a NOT NULL composite key.
ANSWER 3:
Yes, I acknowledge that <parent_location_id, id> is sufficient for the the persistent identity in the class, as well as the primary key constraint for the song_library table.
In addition, the unique constraint be changed to <parent_location_id,name>

primary key constraint:
    parent_location_id,id
unique constraint:
    parent_location_id,name


I'd rather drop parent_folder_id from the PK and keep the PK as just (parent_location_id, id), with parent_folder_id staying a plain FK column that's simply NULL for the ROOT row 
(every other row has it non-null). That matches what id's uniqueness already gives you for free and sidesteps the ROOT-row conflict entirely. Do you want that, or is there a reason 
you want parent_folder_id in the key itself (e.g. some query/index shape) that I should design around instead — in which case how should ROOT be represented?

Yes, per answer #3, I want to drop parent_folder_id from the PK and keep the PK as just (parent_location_id, id)


I want to stress that for all these changes, that the file system repository persistence should be equivalent to database persistence.  To that end, one should be able to "convert"
from one form to another (especially as a unit test)



My answers are inline:
1: Do you want AbstractEntity annotated @MappedSuperclass too (so version maps correctly from its new home), or would you rather version stay on 
AbstractPersistentEntity specifically and be independently added to the future AbstractAssociativeEntity/AbstractLibraryEntity line 
(since that's presumably why you moved it — so composite-key entities get a version too)?
ANSWER 1:
Yes, AbstractEntity should be annotated with @MappedSuperclass too.


What's AbstractRepository's intended role — is its static counter meant to become the shared per-scan SongScanner counter from Decision 1 
(in which case it needs a way to reset to 1 per scan, which seedNextPersistentIdentityFrom currently can't do, since it only ever moves the counter up), 
or is it for something else (e.g. filesystem-repository id-minting across incremental reloads) that I shouldn't conflate with SongScanner's counter?
ANSWER 2:
Ignore AbstractRepository, as the counter in SongScanner from Decision 1 is all that will be needed to get unique integer values.








So when LocationService.getOrCreateOwnLocation() bootstraps the local LocationEntity on first boot, I want to confirm:

Slave mode: create it with persistentIdentity = appProperties.getLocationId() (matching the id master already assigned), not an auto-generated one — otherwise the slave's 
local record and master's record would disagree on id.
ANSWER:
create it with persistentIdentity = appProperties.getLocationId()


Standalone mode (no master, app.location-id not required/set): auto-generate the id the same way registerLocation() does (locationRepository.nextPersistentIdentity(), falling 
back to count+1 for the filesystem store).
ANSWER:
Auto-generate the id the same way registerLocation() does


apiKeyHash on this local record: since nothing ever authenticates into a slave's own local LocationRepository (the real API-key check happens only on master, against 
master's own copy of the location), I'll just generate a random throwaway hash for it (passwordEncoder.encode(<random>)) rather than trying to derive one from 
app.location-api-key — that key is for outbound auth to master, a different secret than what apiKeyHash represents here.
ANSWER:
That is fine with me.

Does that match what you had in mind, or should the local bootstrap path work differently?
Yes, that matches what I had in mind.









Describe the needed REST API to support this mobile app, in terms of a Java 21 Spring Boot controller, as well as any DTOs

3 minutes ago

Planning API endpoints and data structures (6 seconds)

Here's how I'd shape the backend in Java 21 + Spring Boot. Java 21 makes this nice because every DTO can be a record, which keeps the contract tight and immutable.

High-level resource map

The mobile app touches five concerns: catalog (read-only), search, queue, now-playing, and credits. I'd split them into focused controllers under one base path, e.g. /api/v1. The "venue" (a single jukebox unit) is implicit from the authenticated client; multi-tenant deployments would carry a venueId either in the JWT or as a path prefix like /venues/{venueId}/....

GET    /catalog/artists                  list, paged + ?letter=A

GET    /catalog/artists/{id}             artist detail (with albums + top tracks)

GET    /catalog/albums/{id}              album detail (with tracks)

GET    /catalog/genres                   list of genre tiles + counts

GET    /catalog/genres/{slug}/tracks     tracks for a genre (incl. TOP_40, NEW_RELEASES)

GET    /catalog/tracks/popular           "Hot Tracks" rail

GET    /search?q=...                     bundled artists + albums + tracks

GET    /queue                            current queue + now-playing snapshot

POST   /queue                            add a track (returns updated queue + new credit balance)

DELETE /queue/{queueItemId}              remove

PATCH  /queue/{queueItemId}              reorder (body: { newPosition })

POST   /queue/skip                       admin/staff only

GET    /now-playing                      lightweight snapshot for the Now Playing tab

GET    /credits                          balance + recent purchases

POST   /credits/purchase                 buy a pack (idempotent via Idempotency-Key header)

For the live feel (queue updates from other patrons, progress ticks), I'd layer a single STOMP-over-WebSocket endpoint: /ws with topics /topic/venue/{venueId}/queue and /topic/venue/{venueId}/now-playing. The REST endpoints stay authoritative; the socket just pushes deltas.

Controllers

@RestController

@RequestMapping("/api/v1/catalog")

@RequiredArgsConstructor

class CatalogController {

    private final CatalogService catalog;

    @GetMapping("/artists")

    PageResponse<ArtistSummaryDto> listArtists(

            @RequestParam(required = false) String letter,

            @RequestParam(required = false) String q,

            @PageableDefault(size = 50, sort = "name") Pageable pageable) {

        return catalog.listArtists(letter, q, pageable);

    }

    @GetMapping("/artists/{id}")

    ArtistDetailDto artist(@PathVariable UUID id) {

        return catalog.getArtist(id);

    }

    @GetMapping("/albums/{id}")

    AlbumDetailDto album(@PathVariable UUID id) {

        return catalog.getAlbum(id);

    }

    @GetMapping("/genres")

    List<GenreTileDto> genres() { return catalog.listGenres(); }

    @GetMapping("/genres/{slug}/tracks")

    List<TrackSummaryDto> genreTracks(@PathVariable String slug) {

        return catalog.tracksForGenre(slug); // handles TOP_40 / NEW_RELEASES specially

    }

    @GetMapping("/tracks/popular")

    List<TrackSummaryDto> popular(@RequestParam(defaultValue = "8") int limit) {

        return catalog.popularTracks(limit);

    }

}

@RestController

@RequestMapping("/api/v1/search")

@RequiredArgsConstructor

class SearchController {

    private final SearchService search;

    @GetMapping

    SearchResultsDto search(@RequestParam @Size(min = 1, max = 64) String q,

                            @RequestParam(defaultValue = "12") int limit) {

        return search.search(q, limit);

    }

}

@RestController

@RequestMapping("/api/v1/queue")

@RequiredArgsConstructor

class QueueController {

    private final QueueService queue;

    @GetMapping

    QueueSnapshotDto current(@AuthenticationPrincipal Patron patron) {

        return queue.snapshotFor(patron.venueId());

    }

    @PostMapping

    ResponseEntity<QueueAddResultDto> add(@AuthenticationPrincipal Patron patron,

                                          @RequestBody @Valid AddToQueueRequest body) {

        var result = queue.add(patron, body); // throws InsufficientCreditsException -> 402

        return ResponseEntity.status(HttpStatus.CREATED).body(result);

    }

    @DeleteMapping("/{queueItemId}")

    @ResponseStatus(HttpStatus.NO_CONTENT)

    void remove(@AuthenticationPrincipal Patron patron, @PathVariable UUID queueItemId) {

        queue.remove(patron, queueItemId);

    }

    @PatchMapping("/{queueItemId}")

    QueueSnapshotDto reorder(@AuthenticationPrincipal Patron patron,

                             @PathVariable UUID queueItemId,

                             @RequestBody @Valid ReorderRequest body) {

        return queue.reorder(patron, queueItemId, body.newPosition());

    }

    @PostMapping("/skip")

    @PreAuthorize("hasRole('STAFF')")

    QueueSnapshotDto skip(@AuthenticationPrincipal Patron staff) {

        return queue.skipNext(staff.venueId());

    }

}

@RestController

@RequestMapping("/api/v1/now-playing")

@RequiredArgsConstructor

class NowPlayingController {

    private final PlaybackService playback;

    @GetMapping

    NowPlayingDto current(@AuthenticationPrincipal Patron patron) {

        return playback.currentFor(patron.venueId());

    }

}

@RestController

@RequestMapping("/api/v1/credits")

@RequiredArgsConstructor

class CreditsController {

    private final CreditsService credits;

    @GetMapping

    CreditsBalanceDto balance(@AuthenticationPrincipal Patron patron) {

        return credits.balanceFor(patron);

    }

    @PostMapping("/purchase")

    CreditsBalanceDto purchase(@AuthenticationPrincipal Patron patron,

                               @RequestHeader("Idempotency-Key") String idempotencyKey,

                               @RequestBody @Valid CreditPurchaseRequest body) {

        return credits.purchase(patron, body, idempotencyKey);

    }

}

DTOs (Java 21 records)

// ---------- Catalog ----------

public record ArtistSummaryDto(

        UUID id,

        String name,

        long playCount,

        String initial          // for the A-Z rail

) {}

public record ArtistDetailDto(

        UUID id,

        String name,

        String bio,

        long playCount,

        List<String> genres,

        List<AlbumSummaryDto> albums,

        List<TrackSummaryDto> topTracks

) {}

public record AlbumSummaryDto(

        UUID id,

        String title,

        int year,

        URI coverUrl,           // signed URL or CDN path; null -> client uses gradient placeholder

        UUID artistId,

        String artistName

) {}

public record AlbumDetailDto(

        UUID id,

        String title,

        int year,

        URI coverUrl,

        UUID artistId,

        String artistName,

        Duration totalDuration,

        List<TrackSummaryDto> tracks

) {}

public record TrackSummaryDto(

        UUID id,

        String title,

        Duration duration,

        long plays,

        UUID artistId,

        String artistName,

        UUID albumId,

        String albumTitle,

        URI coverUrl,

        String genre

) {}

public record GenreTileDto(

        String slug,            // "ROCK", "TOP_40", "NEW_RELEASES" ...

        String label,

        long trackCount,

        String primaryColor,    // hex, drives the neon tile gradient

        String secondaryColor

) {}

// ---------- Search ----------

public record SearchResultsDto(

        String query,

        List<ArtistSummaryDto> artists,

        List<AlbumSummaryDto> albums,

        List<TrackSummaryDto> tracks

) {}

// ---------- Queue / now-playing ----------

public record QueueItemDto(

        UUID id,

        int position,

        TrackSummaryDto track,

        String addedBy,         // "TABLE 7" / display name

        Instant queuedAt

) {}

public record NowPlayingDto(

        QueueItemDto item,      // null when idle

        Instant startedAt,

        Duration elapsed,

        boolean isPlaying

) {}

public record QueueSnapshotDto(

        NowPlayingDto nowPlaying,

        List<QueueItemDto> upcoming,

        Duration totalUpcomingDuration

) {}

public record AddToQueueRequest(

        @NotNull UUID trackId,

        @Size(max = 32) String addedBy   // optional table/handle

) {}

public record ReorderRequest(

        @Min(0) int newPosition

) {}

public record QueueAddResultDto(

        QueueItemDto added,

        QueueSnapshotDto snapshot,

        int remainingCredits

) {}

// ---------- Credits ----------

public record CreditsBalanceDto(

        int balance,

        Instant updatedAt,

        List<CreditPackDto> availablePacks

) {}

public record CreditPackDto(

        String sku,             // "PACK_1", "PACK_5", "PACK_12"

        int plays,

        String displayPrice,    // "$1", "$4", "$8"

        String currency,        // "USD"

        long priceMinor         // 100, 400, 800

) {}

public record CreditPurchaseRequest(

        @NotBlank String sku,

        @NotBlank String paymentToken   // from Stripe / Apple Pay / etc.

) {}

A few design notes worth flagging

Use records, not Lombok DTOs. Records give you immutability + equals/hashCode/toString for free, and serialize cleanly with Jackson out of the box on Spring Boot 3+.

Money as minor units. priceMinor keeps you out of BigDecimal/double trouble; format on the client.

Idempotency on /credits/purchase. Critical for mobile retries — the Idempotency-Key header lets you safely de-duplicate without double-charging.

Insufficient credits should be HTTP 402 ("Payment Required") via @ControllerAdvice mapping InsufficientCreditsException → ProblemDetail. The current mobile app already 
shows an "OUT OF PLAYS" toast that maps cleanly to that response.

Pagination. Use Spring's Page<T> for /catalog/artists, but wrap it in a leaner PageResponse<T> record so you don't leak Spring's PageImpl JSON shape (which is unstable 
and Spring even warns about it).

Cover art URLs. Return either a CDN URL or null. Letting the client decide to render a gradient placeholder (which it already does) means you don't have to ship actual art
for every album.
 
Genre slugs. Send a stable slug (ROCK, NEW_RELEASES) plus a human label and the two hex colors for the tile. That keeps the mobile theme data-driven instead of hard-coding the palette.

Live updates. Add @MessageMapping controllers backed by Spring's STOMP support so other devices see the queue change in real time. 
The REST endpoints publish to the topic after every mutation.

Auth. A patron is anonymous-but-persistent (a device-bound JWT works well). 

Staff get a STAFF role that unlocks /queue/skip and any future admin endpoints. 

Server-side venueId resolution prevents one bar's queue from leaking into another.

If you want, I can turn this into an OpenAPI spec next so the mobile app could consume generated TypeScript clients and Zod schemas through the same workflow the 
rest of this monorepo already uses.

 
 
 
 Refactor SongQueueRepository to have a JPA implementation.  Use the same pattern as is done in UserService with UserRepositoryJpaImpl.  Do the same thing with SongQueueRootEntity as is done with UserRootEntity in that there is no table for the root entity.  Ask any questions before proceeding.
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 A few design notes worth flagging

DONE Use records, not Lombok DTOs. Records give you immutability + equals/hashCode/toString for free, and serialize cleanly with Jackson out of the box on Spring Boot 3+.

Money as minor units. priceMinor keeps you out of BigDecimal/double trouble; format on the client.

Idempotency on /credits/purchase. Critical for mobile retries — the Idempotency-Key header lets you safely de-duplicate without double-charging.

Insufficient credits should be HTTP 402 ("Payment Required") via @ControllerAdvice mapping InsufficientCreditsException → ProblemDetail. The current mobile app already 
shows an "OUT OF PLAYS" toast that maps cleanly to that response.

Pagination. Use Spring's Page<T> for /catalog/artists, but wrap it in a leaner PageResponse<T> record so you don't leak Spring's PageImpl JSON shape (which is unstable 
and Spring even warns about it).

Cover art URLs. Return either a CDN URL or null. Letting the client decide to render a gradient placeholder (which it already does) means you don't have to ship actual art
for every album.
 
Genre slugs. Send a stable slug (ROCK, NEW_RELEASES) plus a human label and the two hex colors for the tile. That keeps the mobile theme data-driven instead of hard-coding the palette.

Live updates. Add @MessageMapping controllers backed by Spring's STOMP support so other devices see the queue change in real time. 
The REST endpoints publish to the topic after every mutation.

Auth. A patron is anonymous-but-persistent (a device-bound JWT works well). 

Staff get a STAFF role that unlocks /queue/skip and any future admin endpoints. 

Server-side venueId resolution prevents one bar's queue from leaking into another.

If you want, I can turn this into an OpenAPI spec next so the mobile app could consume generated TypeScript clients and Zod schemas through the same workflow the 
rest of this monorepo already uses.




One unrelated bug surfaced along the way (not caused by the DTO refactor, not fixed): GET /api/locations returns 500 instead of 404 in standalone mode, because LocationController isn't registered there (@ConditionalOnProperty(havingValue="master")) and the app's GlobalExceptionHandler catches Spring's NoResourceFoundException under its generic Exception handler. Flagging it sin

MASTER IP ADDRESS:
192.168.1.239